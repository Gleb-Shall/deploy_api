в прод
git init
git add .
git commit -m "initial"
git remote add origin git@45.90.35.151:sites/name.git
git branch -M main
git push -u origin main 2>&1 | tee /dev/tty | grep 'DEPLOY_URL:' | sed 's/.*DEPLOY_URL://' | sed 's/[[:space:]].*//'

изменения
git add .
git commit -m "changes..."
git push 2>&1 | tee /dev/tty | grep 'DEPLOY_URL:' | sed 's/.*DEPLOY_URL://' | sed 's/[[:space:]].*//'


<!-- в dev
git init
git add .
git commit -m "initial"
git remote add origin git@45.90.35.151:sites/ИМЯ_РЕПО.git
git branch -M dev
git push -u origin dev -->